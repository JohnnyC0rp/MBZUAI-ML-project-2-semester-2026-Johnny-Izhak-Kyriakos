# Overleaf Connection Test

This repository contains a minimal LaTeX project for testing GitHub and Overleaf integration.

Files:

- `main.tex` — a small compileable test document
- `.gitignore` — ignores local LaTeX build artifacts

The goal is simple: import or sync this repository with Overleaf and confirm the document compiles cleanly.
